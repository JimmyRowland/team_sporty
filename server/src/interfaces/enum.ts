export enum SportEnum {
    football = "football",
    cricket = "cricket",
    basketball = "basketball",
    unspecified = "",
}

export enum EventUserResEnum {
    notGoing,
    going,
    noResponse,
}

export enum EventTypeEnum {
    match = "match",
    training = "training",
}
